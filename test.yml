spring:
application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/fkccoding/config-repo
          username: fkccoding
          password: 1996228fkcgi
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8762/eureka/
